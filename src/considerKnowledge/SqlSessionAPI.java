package considerKnowledge;

/**********************SqlSession API接口**********************
 *session对象是hibernate框架的核心，包含一些数据持久化的操作接口
 *************************session方法**************************
 *1.sqlSessionAPI.save(Object object):保存java对象到数据库
 *2.sqlSessionAPI.delete(Object object):删除一个对象
 *3.sqlSessionAPI.load(entityName,id):通过主键id获取持久化的实体类对象，id为主键值
 *4.sqlSessionAPI.get(entityName,id):通过主键id获取持久化的实体类对象，id为主键值
 *5.sqlSessionAPI.update(Object object):修改一个对象
 **/
public class SqlSessionAPI {

}
